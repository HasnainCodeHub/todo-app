# Phase II Backend Dependencies
# CRITICAL: Do NOT install 'jwt' package - only PyJWT

# Core Framework
fastapi>=0.115.0
uvicorn[standard]>=0.32.0

# Database
sqlmodel>=0.0.22
asyncpg>=0.30.0

# Authentication (Phase II)
# WARNING: PyJWT MUST be pinned - 'jwt' package causes import collision
PyJWT==2.8.0
passlib[bcrypt]>=1.7.4
email-validator>=2.1.0

# Configuration & Environment
python-dotenv>=1.0.0
pydantic-settings>=2.6.0

# Utilities
python-dateutil>=2.9.0

# Development & Testing
pytest>=8.3.0
pytest-asyncio>=0.24.0
httpx>=0.28.0
aiosqlite>=0.20.0
