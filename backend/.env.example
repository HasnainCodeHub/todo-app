# Todo API Backend - Environment Configuration
# Phase II: Full-Stack Web App with Auth
# Copy this file to .env and fill in your actual values

# =============================================================================
# DATABASE CONNECTION (Required)
# =============================================================================
# Railway PostgreSQL: Use the DATABASE_URL provided by Railway
# Local PostgreSQL example:
#   DATABASE_URL=postgresql://todo_user:password@localhost:5432/todo_app
#
DATABASE_URL=postgresql://todo_user:password@localhost:5432/todo_app

# =============================================================================
# AUTHENTICATION (Required)
# =============================================================================
# Secret key for JWT HS256 signature verification.
# Generate a secure 256-bit key: openssl rand -base64 32
#
# WARNING: Do NOT commit your actual secret to version control!
#
BETTER_AUTH_SECRET=your-secret-key-here-generate-with-openssl

# =============================================================================
# CORS CONFIGURATION (Required for Production)
# =============================================================================
# Comma-separated list of allowed frontend origins
# For Railway: Add your frontend Railway URL
#
# Local development:
CORS_ORIGINS=http://localhost:3000,http://127.0.0.1:3000
#
# Production example:
# CORS_ORIGINS=https://your-frontend.up.railway.app,http://localhost:3000

# =============================================================================
# OPTIONAL SETTINGS
# =============================================================================
APP_NAME=Todo API
DEBUG=False

# =============================================================================
# TESTING FLAGS (Optional - for local development)
# =============================================================================
# TESTING=0
# INTEGRATION_TESTS=0
