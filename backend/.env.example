# Todo API Backend - Environment Configuration
# Phase II: Full-Stack Web App with Auth
# Copy this file to .env and fill in your actual values

# =============================================================================
# DATABASE CONNECTION (Required)
# =============================================================================
# The app accepts both sync and async URL formats - auto-converts to async internally.
#
# IMPORTANT: Special characters in password must be URL-encoded:
#   @ -> %40
#   # -> %23
#   : -> %3A
#   / -> %2F
#
# Local PostgreSQL example:
#   DATABASE_URL=postgresql://todo_user:password@localhost:5432/todo_app
#
# Local PostgreSQL with special chars (password: @Hasnain524225):
#   DATABASE_URL=postgresql://todo_user:%40Hasnain524225@localhost:5432/todo_app
#
# Neon (cloud) PostgreSQL example:
#   DATABASE_URL=postgresql://user:password@ep-cool-darkness-123456.us-east-2.aws.neon.tech/neondb?sslmode=require
#
DATABASE_URL=postgresql://todo_user:password@localhost:5432/todo_app

# =============================================================================
# AUTHENTICATION (Required for Phase II)
# =============================================================================
# Secret key for JWT HS256 signature verification.
# Must match the secret used by Better Auth on the frontend.
# Generate a secure 256-bit key: openssl rand -base64 32
#
# WARNING: Do NOT commit your actual secret to version control!
#
BETTER_AUTH_SECRET=your-256-bit-secret-key-here-replace-me

# =============================================================================
# APPLICATION SETTINGS (Optional)
# =============================================================================
APP_NAME=Todo API
DEBUG=False

# =============================================================================
# TESTING FLAGS (Optional)
# =============================================================================
# Set TESTING=1 to use SQLite in-memory database for unit tests
# Set INTEGRATION_TESTS=1 to run integration tests against real database
#
# TESTING=0
# INTEGRATION_TESTS=0
