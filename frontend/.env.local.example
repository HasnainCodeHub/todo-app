# Frontend Environment Configuration
# Phase II: Full-Stack Web App with Auth
# Copy this file to .env.local and fill in your values

# =============================================================================
# API Configuration
# =============================================================================
# Backend API base URL (without trailing slash)
# Default: http://127.0.0.1:8001/api
#
NEXT_PUBLIC_API_BASE_URL=http://127.0.0.1:8001/api

# =============================================================================
# Better Auth Configuration (for production)
# =============================================================================
# The secret must match the backend's BETTER_AUTH_SECRET for JWT verification
#
# BETTER_AUTH_SECRET=your-256-bit-secret-key-here

# =============================================================================
# Development Notes
# =============================================================================
# For local development without Better Auth:
#
# 1. Start the backend:
#    cd backend && uvicorn app.main:app --reload --port 8001
#
# 2. Generate a test token (in backend directory):
#    python -c "from app.auth import create_test_token; print(create_test_token('demo_user'))"
#
# 3. Set the token in browser console:
#    localStorage.setItem('auth_token', '<token-from-step-2>')
#    localStorage.setItem('user_id', 'demo_user')
#
# 4. Refresh the page to use authenticated endpoints
